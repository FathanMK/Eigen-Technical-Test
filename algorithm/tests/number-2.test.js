const longestWord = require("../questions/number-2");

test("mengerjakan as the longest word", () => {
  expect(longestWord("Saya sangat senang mengerjakan soal algoritma")).toBe(
    "mengerjakan"
  );
});
